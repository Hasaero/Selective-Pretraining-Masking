# RHO-CM pretraining — pinned dependencies (Python 3.10, CUDA 12.4)
# Install PyTorch separately first:
#   pip install torch==2.4.1 torchvision==0.19.1 \
#       --index-url https://download.pytorch.org/whl/cu124

# ── Time-series foundation models ──────────────────────────────────────────
momentfm==0.1.4
uni2ts==2.0.0
chronos-forecasting==1.5.0          # transitive dep; pinned for compatibility

# ── HuggingFace stack (pinned by momentfm 0.1.4) ───────────────────────────
transformers==4.33.3
huggingface-hub==0.24.0
tokenizers<0.14
datasets>=2.17.0,<3
safetensors

# ── Numerics & data ────────────────────────────────────────────────────────
numpy>=1.25,<2
pandas>=2.0,<3
scipy>=1.11
scikit-learn>=1.3

# ── Utilities ──────────────────────────────────────────────────────────────
einops>=0.7
matplotlib>=3.7
tqdm>=4.65
PyYAML>=6.0

# ── uni2ts transitive deps (kept explicit for reproducibility) ─────────────
gluonts>=0.14,<0.15
hydra-core>=1.3
omegaconf>=2.3
jaxtyping>=0.2.30
lightning>=2.0
